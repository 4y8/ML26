\documentclass[sigplan, nonacm, natbib = false, review, screen]{acmart}

\usepackage{listings}

\lstset{
  basicstyle=\ttfamily\footnotesize,
  columns=fullflexible,
  keepspaces=true
}

\lstdefinestyle{metl}{
  language=[Objective]Caml,
  morekeywords={eff, handle, end, return, mask, forall, Effect, alias},
  morecomment=[l]{--},
  literate=
    {->}{{$\to$}}2
    {=>}{{$\Rightarrow$}}2
    {()}{{()}}2
}

\lstdefinestyle{koka}{
  morekeywords={fun,rec,handler,forall},
  sensitive=true,
  morecomment=[l]{--},
  morestring=[b]",
  literate=
    {->}{{$\to$}}2
    {()}{{()}}2
}

\usepackage[backend=bibtex, style=numeric]{biblatex}
\addbibresource{abstract.bib}
\renewcommand*{\bibfont}{\footnotesize}

\usepackage{mathpartir}

%% Ghosts

\usepackage{halloweenmath}
\makeatletter
\newcommand*\forallghostraise{.16ex} % vertical nudge upwards
\newcommand*\forallscale{0.4}       % scale factor for ∀

\newcommand{\forallghost}{\mathord{\mathpalette\forallghost@{}}}
\newcommand{\forallghost@}[2]{%
  \ooalign{%
    % ghost outline
    \hfil$\m@th#1\mathpalette\raiseghost\relax$\hfil\cr
    % smaller forall symbol, raised
    \hfil\raise\forallghostraise\hbox{%
      \scalebox{\forallscale}{$\m@th#1\forall$}}%
    \hfil\cr
  }%
}

\makeatother


\newcommand{\myghost}{\mathpalette\raiseghost\relax}
\newcommand{\raiseghost}[2]{\raisebox{.5\depth}{\scalebox{0.9}{$#1\mathghost$}}}

\newcommand{\Hole}{{\myghost}}
\newcommand{\TopHole}{\forallghost} % implicit forall

%%

%% Keywords

\newcommand{\keyw}[1]{{\textbf{{\textsf{#1}}}}}
\newcommand{\Do}{\keyw{do}\;}

\usepackage{xspace}
\newcommand{\Met}{\textsc{Met}\xspace}
\newcommand{\Metl}{\textsc{Metl}\xspace}
%% AB: probably change this name
\newcommand{\MetlFrost}{$\textsc{Metl}^{\text{Frost}}$\xspace}
%%

%% Margin notes.
%% For submission, comment all the following lines out ...
\paperwidth    =\dimexpr\paperwidth     + 4cm\relax
\oddsidemargin =\dimexpr\oddsidemargin  + 2cm\relax
\evensidemargin=\dimexpr\evensidemargin + 2cm\relax
\marginparwidth=\dimexpr\marginparwidth + 2.2cm\relax
\usepackage{todonotes,setspace}
\newcommand{\todobox}[3]{{\todo[color=#1!20,bordercolor=#1!80]{%
  \begin{spacing}{0.9}\footnotesize\textbf{#2:} #3\end{spacing}}}}
\newcommand{\wenhao}[1]{\todobox{blue}{Wenhao}{#1}}
\newcommand{\aghilas}[1]{\todobox{red}{Aghilas}{#1}}
\newcommand{\sam}[1]{\todobox{yellow}{Sam}{#1}}
%% ... up to here

\title{Practical programming with Modal Effect Types}
\subtitle{Research Presentation}
\settopmatter{authorsperrow=3}

\author{Aghilas Y. Boussaa}
\orcid{0009-0005-5954-1711}
\email{aghilas.boussaa@ens.fr}
\affiliation{
  \institution{École normale supérieure -- PSL}
  \country{France}
}

\author{Wenhao Tang}
\orcid{0009-0000-6589-3821}
\email{wenhao.tang@ed.ac.uk}
\affiliation{%
  \institution{The University of Edinburgh}
  \country{UK}
}

%% \author{Shengyi Jiang}
%% \orcid{0000-0002-4443-0753}
%% \email{syjiang@cs.hku.hk}
%% \affiliation{%
%%   \institution{The University of Hong Kong}
%%   \country{China}
%% }

%% \author{Bruno C. d. S. Oliveira}
%% \orcid{0000-0002-1846-7210}
%% \email{bruno@cs.hku.hk}
%% \affiliation{%
%%   \institution{The University of Hong Kong}
%%   \country{China}
%% }

\author{Sam Lindley}
\orcid{0000-0002-1360-4714}
\email{sam.lindley@ed.ac.uk}
\affiliation{%
  \institution{The University of Edinburgh}
  \country{UK}}

\begin{document}
\sloppy
\begin{abstract}
  % Effect handlers are a powerful programming construct allowing users
  % to define and combine computational effects.
  %
  Effect systems statically track the effects that a program can
  perform.
  %
  Traditional effect systems rely on effect variables to provide
  modular type signatures for higher-order functions.
  %
  Modal effect types use modalities to give modular and succinct types
  to higher-order functions, reducing the need for effect variables
  meanwhile still allowing reusing higher-order functions to different
  effectful arguments.
  %
  However, the practical implement of modal effect types, in
  particular how to infer modalities, remains largely unexplored.

  We overview our implementation of a toy ML-like programming language
  with modal effect types.
  %
  Our main novelty is that we extend Frost, a new type inference
  algorithm for first-class polymorphism, to infer first-class
  modalities.
\end{abstract}
\maketitle
\section{Introduction}
\subsection{Existing type-and-effect systems}
To take effects into account in the type system, traditional languages such as
Koka~\cite{koka2014} annotate arrows with the effects the function can
perform. Therefore, each function inherits, in its signature, the effects of its
callees. This becomes cumbersome when typing higher-order functions and is an
obstacle to backward compatibility. For instance, if we annotate arrows with the
effects they can perform between square brackets, the function \lstinline{map} has
the following type, even though it does not perform any effect itself.
\begin{lstlisting}[style=metl]
  forall e . (list a, a ->[e] b) ->[e] list b
\end{lstlisting}
The effect variable \lstinline{e} allows \lstinline{map} to take any effectful
function as argument. Syntactic sugar can simplify such signatures, as
demonstrated by Frank~\cite{frank2020}. But relying on syntax makes the system
fragile.

The language Effekt~\cite{effekt2020} solves this problem with a
\emph{capability} based system. However, to guarantee effect-safety, functions
become second-class objects, which hinders backward compatibility.

\subsection{Modal Effect Types}

\sam{We need to explain \emph{how} the modality $[]$ modifies the
  ambient effect context here - and in particular how conceptually it
  applies to all of the arrows contained in its scope.}

Modal effect types (\Met) remove the need for effect variables by not tying
effects to arrows ~\cite{tang2025modal, tang2026modal}. In \Met,
%draws inspiration from Multimodal Type Theory~\cite{mtt2020} and
\emph{effect contexts} represent the effects a computation can perform;
\emph{modalities} then modify effect contexts. For instance, the \textsf{map}
function has the following type, where the modality $[]$ indicates that the
function does not perform any effect.
\[
  \forall a b . []((a \to b) \to \textsf{list } a \to \textsf{list } b)
\]
The modality $[]$ is \emph{absolute}, which means that it replaces the effect
context with an independent one; here the empty context. For an effect context
$E$, the modality $[E]$ replaces the effect context by $E$. In the example
above, the modality implies that if any of the arrow in the type of \textsf{map}
performs an effect, then this effect comes from the ambient context of one of
the parent computations.
%
There is another kind of modality: the \emph{relative} modality $\langle
L|D\rangle$ remove the effects in $L$ from the ambient effect context before
adding the effects in $D$.

At the term level, the $\keyw{mod}_\mu$ and $\keyw{let mod}_\mu$ constructs
respectively introduce and eliminate modality $\mu$. For example, this
expression performs explicit subeffecting of a variable $x:[](\textsf{int}
\to\textsf{int})$ in any effect context:
\[
\left(\keyw{let mod}_{[]} y = x \keyw{ in } \keyw{mod}_{[E]} y\right) : [E](\textsf{int} \to\textsf{int})
\]
The $\keyw{mod}_\mu V$ expression modifies the effect context of the value $V$
by applying the modality $\mu$. Let-bindings allow to retreive the original
value: $\keyw{let mod}_\mu x = \keyw{mod}_\mu V \keyw{ in } x$ reduces to $V$.
%
See~\cite{tang2025modal, tang2026modal} for a complete introduction to modal
effect types.

Therefore modalities require explicit boxing and unboxing, and \Met needs an
inference algorithm to become a programming language. The original
paper~\cite{tang2025modal} provides such an algorithm that translates a surface
language called \Metl to \Met. But this algorithm only deals with modalities:
\Metl requires explicit type application for value polymorphism.

\sam{The reader isn't going to know that modalities require explicit
  boxing and unboxing. This needs explaining.}

% AB: removed the paragraph
%\sam{I'm not clear on when, where or why we need a type annotation
%  here.}

\subsection{Frost type inference}
The usual way to type check an application with a bidirectional type system is
to, first, infer the type of the function and then use this type to check the
argument~\cite{dunfield2013complete, greedy2023, jiang2025bidirectional}. Less
commonly, some type systems allow the information to flow from the argument to
the function~\cite{pierce2000local, xie2018let}.

% AB: should be fixed
\sam{Fewer type systems? I think we mean something like ``Less
  commonly, some type systems...''}

Frost supports information flow in both directions. To do so, the type system relies
on \emph{skeletons}. Skeletons are types haunted by \emph{ghosts} ($\Hole$) and
\emph{universal ghosts} ($\TopHole$), i.e.\ skeletons are types with holes in
type or quantifier position. When type checking an application, the rule infers
a skeleton for the argument, uses that skeleton to infer a type for the function
and checks the argument against the function type's argument. The skeleton allows
information flow from the argument to the function before it flows in the other
direction.

For instance, consider two variables $\textsf{poly'}:\forall a.(\forall b. b\to
b\times a)\to a$ and $\textsf{ids}:\textsf{list }(\forall a.a\to a)$. Frost
infers the type $\textsf{list }(\forall a.a\to a)$ for the expression
$\textsf{poly'} (\lambda x. (x, \textsf{ids}))$.
%
First, the skeleton $\TopHole.\Hole\to\Hole\times(\TopHole.\textsf{list
}(\forall a.a\to a))$ of the argument instantiate the function's type variable
$a$ to $\textsf{list }(\forall a.a\to a)$.
%
Then, checking the argument against $\forall b.b\to b\times\textsf{list
}(\forall a.a\to a)$ wraps the argument in a type abstraction.

% AB: should be fixed
\sam{``remind'' should presumably be ``is reminiscent of''} As noted in previous
work on \Met, elimination and introduction of modalities is reminiscent of
instantiation and generalisation in type inference~\cite[Section
  7]{tang2025modal}. Universal ghosts unify the treatment of modalities and
quantifiers.

% AB: should be fixed
\sam{``parallel''?}

We will review a toy programming language which extends Frost to infer modal
effect types, which we call \MetlFrost. Examples in \Met, \Metl and \MetlFrost
will demonstrate the capabilities of the latter. Then, other examples will show
some limitations of \MetlFrost. More examples, as well as a test suite are
available
\href{https://github.com/4y8/modal-effect-types/tree/frost}{online}.

We lack proofs of completeness and soundness for the extension of Frost to modal
effect types.
% AB: should be fixed
\sam{I'm intrigued by seediness :)}

\section{Practical programming with Modal Effect Types}
\subsection{Our implementation}
The core language of our implementation is \Met with scoped
rows~\cite{scopedrows2005} as an effect structure (i.e.\ modalities will have
scoped rows of effects) with additions such as parametric effects and algebraic
data types.

\aghilas{I am not sure of how to integrate these paragraphs, if they are
  relevant} \Met has modality-annotated variable declarations in its contexts:
$x:_\mu A$, where $A$ can also have a modality. For simplicity's sake, the
\MetlFrost, like \Metl, has usual plain bindings, and unboxes all the modalities
of variables.

\subsection{Iterating on a list}
The standard iter higher-order function has type $\forall a . []((a \to
\textsf{unit})\to \textsf{unit})$ in \Met. To call itself, the function unboxes
the absolute modality of $\textsf{iter}\, @a$, where the at sign indicates type
application.  \aghilas{not sure about the syntax we should use here}
\[
\begin{aligned}
\Lambda a^{\textsf{Any}}.\,
&\keyw{mod}_{[]}.\lambda f^{a\to\textsf{unit}} l^{\textsf{list }a}. \\
&\keyw{match } l \keyw{ with } \\
&\quad \mid \textsf{Nil} \to () \\
&\quad \mid \textsf{Cons}(hd,tl) \to
   \keyw{let\ mod}^{\langle\rangle}\_ = f\,hd \keyw{\ in } \\
&\qquad\qquad
   \keyw{let\ mod}^{[]} i = \textsf{iter}\, @a
   \keyw{\ in } \\
&\qquad\qquad
   i\,f\,tl
\end{aligned}
\]
%% \[
%% \Lambda a^{\textsf{Any}}.\keyw{mod}_{[]}\lambda f^{a\to\textsf{unit}} l^{\textsf{list }a}.\keyw{match } l \keyw{ with }\textsf{Nil}\to () \mid \textsf{Cons}(hd, tl)\to
%% \keyw{let mod}^{\langle\rangle}\_ = f\; hd \keyw{ in }
%% \keyw{let mod}^{[]}i = \textsf{iter}\; \{ a \} \keyw{ in }
%% i\; f\; tl
%% \]

The type in \Metl and \MetlFrost is the same as the \Met one. The bidirectionnal
type system of \Metl avoids explicit modality unboxing.
\begin{lstlisting}[style=metl]
  val iter : forall a . []((a -> unit) -> unit)
  let iter f l =
    match l with
    | Nil -> ()
    | Cons (hd, tl) -> f hd; iter @a f tl
    end
\end{lstlisting}
And \MetlFrost removes the type application.
\begin{lstlisting}[style=metl]
    | Cons (hd, tl) -> f hd; iter f tl
\end{lstlisting}

% AB: should be fixed, got confused because of the function's name
\sam{I think ``Finding in a'' means ``Searching a''}

\subsection{Searching a list with effects}
Calling the iterator from the last subsection with an effectful function as
argument can search a list, thanks to the parametric effect below.
\begin{lstlisting}[style=metl]
  eff gen [a] = yield : a => unit
\end{lstlisting}
The declaration above defines a parametric effect \lstinline[style=metl]{gen}
with a single operation \lstinline[style=metl]{yield}. The brackets around the
parameter \lstinline[style=metl]{a} indicate that it has kind \textsf{Abs}: the
kind of types whose meaning is independent from the ambient effect
context. There is another kind, called \textsf{Any}, that includes all types. To
avoid effect leakage, the types of inputs and outputs of operations have kind
\textsf{Abs}.

Once again, \Metl eliminates the need of modality boxing and unboxing.
\begin{lstlisting}[style=metl]
  val find :
    forall [a] . []((a -> bool) -> list a -> option a)
  let find p l =
    handle<gen a> iter @a
      (fun x -> if mask<gen> (p x) then do yield x else ())
      l with
    | return _ => None
    | yield x k => Some (x)
    end
\end{lstlisting}
The angle brackets that follow the \lstinline[style=metl]{handle} keyword
provide a parameter for the effect \lstinline[style=metl]{gen}. Frost erases the
need for this annotation, as well as for the explicit type application.

\subsection{Some limitations}
\paragraph{Syntactic treatment of quantifiers}
Our current unification rules have a syntactical treatment of quantifiers.  We
can unify neither $[l]\langle l'\rangle(\textsf{unit}\to\textsf{unit})$ and
$[l', l](\textsf{unit}\to\textsf{unit})$, nor $\forall a b . a \times b$ and
$\forall b a . a \times b$, while they are both subtype of each other in both
cases.

\paragraph{Lack of kind inference}
The type inference rule for lambda abstractions introduces a fresh type variable
for the argument. As the system lacks kind inference, the kind of this variable
is \textsf{Any} to be permissive. Therefore, functions that have to take pure
values need an annotation. For instance, the \lstinline[style=metl]{find}
function of the previous subsection does not type check without the top-level
annotation because the variable \lstinline[style=metl]{x} is not pure.

\subsection{Conclusion and future work}
The extension of Frost to modal effect types is a promising approach to
integrate effect handlers in functional programming languages.

Future work includes taking effect contexts into account in skeleton inference,
extending Frost to support a fragment of system $F^\omega$, and exploring a more
semantic treatment of quantifiers and modalities.

\printbibliography

\end{document}

% LocalWords:  polymorphism Koka callees forall effectful Effekt iter
% LocalWords:  subeffecting instantiation hd tl bool
% LocalWords:  metl subtype
